export interface LibraryModel {
  sitNo: number;
  firstName: string;
  lastName: string;
  whatsappNo: string;
  idProof: string;
  joinDate: string;      // or Date type if you want
  depositeAmount: number;
}
